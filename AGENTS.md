# 프로젝트 목표

- 이 프로젝트는 IFS-informed 자기대화 과정에서 사용자가 표현한 내적 경험을 구조화하고, 이후 추상적 3D 외부화 표현으로 연결하기 위한 웹 기반 연구 프로토타입이다.
- 연구의 핵심은 특정 LLM 모델의 성능 자체가 아니라, 사용자 입력 방식과 프롬프트 구조가 내적 경험의 구조화된 표현에 어떤 영향을 주는지 탐색하는 데 있다.
- 현재 우선순위는 작동 가능한 최소 프로토타입을 빠르게 구현하고, 단계별로 검증하면서 확장하는 것이다.

# 현재 MVP 범위

- 현재 MVP는 다음 흐름을 우선 구현한다.
  1. React 화면에서 고정된 자기탐색 질문을 제시한다.
  2. 사용자의 응답을 입력받는다.
  3. 백엔드를 통해 로컬 LLM에 응답을 전달한다.
  4. LLM이 반환한 구조화된 JSON 결과를 화면에서 확인한다.
- 명시적으로 요청받기 전까지 다음 기능은 먼저 구현하지 않는다.
  - 회원가입 또는 로그인
  - 데이터베이스 저장
  - 배포 설정
  - 복잡한 상태 관리 라이브러리 도입
  - Three.js 또는 React Three Fiber 기반 3D 렌더링
  - 사용자 응답에 대한 치료적 평가 또는 진단 기능

# 연구 설계 원칙

- 이 시스템은 임상 진단 도구나 치료 도구가 아니라, IFS의 자기탐색 흐름을 참고한 연구용 프로토타입이다.
- 질문 문장은 연구 설계의 일부이므로, LLM이 질문을 임의로 생성하거나 수정하지 않도록 한다.
- 질문 단계는 코드에서 명시적으로 관리하며, 각 단계의 역할이 구분되어야 한다.
  - Find: 사용자가 주의를 기울일 내적 부분을 탐색한다.
  - Focus: 사용자가 해당 부분에 주의를 기울였을 때의 경험을 표현한다.
  - Flesh out: 이후 필요한 경우 형태, 거리, 움직임 등 외부화 속성을 구체화한다.
- Find와 Focus 응답만 처리하는 단계에서는, 모델이 색상·형태·움직임·거리 같은 3D 시각 속성을 임의로 추론하지 않도록 한다.
- 시각 속성 생성은 Flesh out 단계가 포함된 이후, 명시적으로 요청된 작업에서만 구현한다.
- LLM은 사용자가 직접 작성한 표현을 구조화하는 역할만 하며, 사용자의 상태를 진단하거나 원인을 단정하거나 치료적 조언을 생성하지 않는다.

# 기술 스택

- 프론트엔드: React
- 언어: 기존 프로젝트 설정을 우선 확인하고, TypeScript 프로젝트이면 TypeScript를 유지한다.
- 개발 서버 및 빌드 도구: 기존 프로젝트의 Vite 설정을 우선 유지한다.
- 백엔드: 기존 백엔드가 있으면 해당 구조를 유지하고, 없을 경우에만 최소한의 Node.js / Express 서버 추가를 제안한다.
- 로컬 LLM 실행 환경: Ollama
- 현재 연결 대상 모델: `qwen3:4b-instruct-2507-q4_K_M`
- LLM 출력 형식: Ollama Structured Outputs와 JSON Schema를 이용한 구조화된 JSON
- 향후 시각화 확장 후보: Three.js 또는 React Three Fiber

# 시스템 구조 원칙

- 브라우저의 React 코드에서 Ollama 또는 외부 LLM API를 직접 호출하지 않는다.
- 프론트엔드는 사용자 입력 수집과 결과 표시를 담당한다.
- 백엔드는 모델 호출, 입력 검증, JSON 파싱, 오류 처리를 담당한다.
- 모델명, API base URL, endpoint처럼 변경될 수 있는 값은 가능한 한 한곳에서 관리한다.
- LLM 요청과 응답의 데이터 구조는 명시적인 타입 또는 스키마로 관리한다.
- 프론트엔드와 백엔드 사이의 요청/응답 구조를 임의로 변경하지 말고, 변경이 필요하면 먼저 이유와 영향 범위를 설명한다.

# 개인정보 및 안전 규칙

- 사용자 응답은 심리적 경험과 관련된 민감한 서술이 될 수 있으므로 불필요하게 저장하거나 출력하지 않는다.
- 사용자의 원문 응답을 `console.log`, 서버 로그, 디버깅 출력에 남기지 않는다.
- 명시적 요청 없이 데이터베이스나 파일에 사용자 응답을 저장하지 않는다.
- 이름, 학번, 연락처 등 식별정보를 입력받는 기능을 추가하지 않는다.
- 사용자에게 진단, 치료 권고, 위험도 판단처럼 보이는 문구를 생성하지 않는다.

# UI 및 사용자 흐름 규칙

- 질문 단계와 사용자 응답 흐름은 단순하고 명확하게 유지한다.
- 한 화면 또는 한 단계에서 사용자가 해야 할 행동이 명확해야 한다.
- 로딩 중, 성공, 오류 상태를 화면에 구분하여 표시한다.
- 연구 설계상 고정된 질문 문장은 별도 상수 또는 설정 파일에서 관리하여 추후 수정 가능하도록 한다.
- 모델 출력 JSON은 초기 개발 단계에서 사용자가 확인할 수 있도록 화면에 명확하게 표시한다.

# LLM 및 JSON 출력 규칙

- LLM 호출 시 system prompt에는 다음 제한이 반영되어야 한다.
  - 사용자의 표현을 간단히 구조화한다.
  - 진단하거나 치료적으로 해석하지 않는다.
  - 입력에 없는 내용을 과도하게 추론하지 않는다.
  - 현재 단계 범위를 벗어난 시각 속성을 생성하지 않는다.
- 구조화 출력이 필요한 기능에서는 JSON Schema를 사용한다.
- 모델 응답은 반드시 파싱 오류를 처리해야 한다.
- 모델이 설치되지 않았거나 Ollama가 실행되지 않은 경우, 사용자가 이해할 수 있는 오류 메시지를 반환한다.
- 모델 출력 필드는 기능 목적에 필요한 최소 범위만 포함한다.

# 코딩 규칙

- 먼저 현재 프로젝트 구조, `package.json`, 기존 컴포넌트와 스타일 방식을 확인한 뒤 수정한다.
- 기존 코드 스타일과 폴더 구조를 최대한 유지한다.
- 기능 구현에 필요한 최소한의 파일만 수정한다.
- 명시적 요청 없이 전체 구조 변경, 대규모 리팩토링, 새로운 상태 관리 라이브러리 도입을 하지 않는다.
- 새 의존성이 필요하면 설치하기 전에 필요 이유와 대체 가능성을 먼저 설명한다.
- 함수와 컴포넌트는 한 가지 책임을 중심으로 작게 유지한다.
- 의미가 분명한 변수명과 함수명을 사용한다.
- 타입 오류를 숨기기 위해 `any`를 임의로 사용하지 않는다.
- 코드 주석이 필요한 경우, 해당 코드 바로 윗줄에 간결하게 작성한다.
- 사용자가 별도 언어를 지정하지 않으면 설명과 문서는 한국어로 작성한다.

# 테스트 및 검증 규칙

- 기능 구현 후 실행 가능한 검증 명령을 직접 실행한다.
- 기존 프로젝트에 lint, typecheck, test, build 명령이 있다면 관련 변경 후 실행한다.
- 최소 연결 테스트에서는 다음 흐름이 정상 작동하는지 확인한다.
  1. 사용자가 질문에 응답을 입력할 수 있다.
  2. 빈 입력은 모델 호출 전에 차단된다.
  3. 프론트엔드가 백엔드 endpoint에 요청을 보낸다.
  4. 백엔드가 Ollama 모델을 호출한다.
  5. 모델 결과가 예상 JSON 구조로 파싱된다.
  6. 성공 결과 또는 오류 상태가 화면에 표시된다.
- 외부 모델 연결이 실제 환경 문제로 테스트되지 못하면, 무엇을 검증했고 무엇을 검증하지 못했는지 명확히 보고한다.

# Git 및 변경 관리 규칙

- 사용자가 명시적으로 요청하지 않으면 Git commit, push, branch 생성, pull request 생성을 하지 않는다.
- 기능 구현과 직접 관련 없는 코드, 주석, 파일 서식은 임의로 수정하지 않는다.
- 변경 후에는 수정한 파일 목록과 각 파일의 역할을 정리한다.
- 기술 스택이나 시스템 구조가 실제로 변경되면, 이 `AGENTS.md`에서 관련 항목도 함께 업데이트할 필요가 있는지 사용자에게 알린다.

# AI 에이전트 작업 규칙

- 작업을 시작하기 전에 현재 코드베이스 구조와 실행 방법을 먼저 확인한다.
- 요청 범위가 명확하면 바로 구현하되, 연구 설계에 영향을 주는 모호한 결정은 추측해서 확정하지 않는다.
- 특히 다음 항목은 사용자 확인 없이 임의로 결정하지 않는다.
  - 질문 문장 변경
  - 실험 조건 변경
  - 시각 속성 매핑 기준 변경
  - 모델 교체
  - 사용자 응답 저장 방식 추가
- 명령 수행에 필요한 최소 범위만 구현한다.
- 현재 단계에서 필요하지 않은 미래 기능을 미리 구현하지 않는다.
- 완료 시 다음 내용을 보고한다.
  1. 구현 또는 수정한 내용
  2. 변경한 파일 목록
  3. 실행 및 테스트 방법
  4. 아직 구현하지 않은 범위
  5. 발생 가능한 다음 작업 후보